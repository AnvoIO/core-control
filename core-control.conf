# core-control global configuration
# Edit these paths to match your environment.
# setup.sh will overwrite this file with detected values.

# Path to the core-node repository
CORE_NODE_DIR=/opt/dev/core-node

# Root data directory (BTRFS recommended)
DATA_ROOT=/data

# SSH port (for UFW setup)
SSH_PORT=22
